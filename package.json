{
  "name": "car-bath",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/write-version.mjs --dev",
    "dev": "wrangler pages dev ./static",
    "predeploy": "node scripts/write-version.mjs",
    "deploy": "wrangler pages deploy ./static --project-name=car-bath",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260710.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.111.0"
  }
}
